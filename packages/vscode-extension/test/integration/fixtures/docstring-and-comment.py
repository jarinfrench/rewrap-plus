def f():
    """A docstring long enough that it needs to be wrapped across multiple lines to fit under the column limit."""
    # this is a very long comment that should definitely need to be wrapped because it exceeds the column limit
    return 1
