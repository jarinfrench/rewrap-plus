# Project Title

This is a fairly long paragraph of introductory prose that will need wrapping under a narrow column limit for sure.

```bash
echo "this is inside a fenced code block and must never be touched no matter how long this line is"
```

<!-- rewrap: off -->

This paragraph is inside the off range and must stay untouched even though it is long enough to need wrapping under the limit for sure.

<!-- rewrap: on -->
