{
  "name": "rewrap-plus",
  "displayName": "Rewrap+",
  "version": "0.1.0",
  "private": true,
  "description": "Rewraps comments, docstrings, and string literals to a configured column limit, preserving formatted structure and emitting language-valid concatenation.",
  "publisher": "jarinfrench",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "https://github.com/jarinfrench/rewrap-plus.git",
    "directory": "packages/vscode-extension"
  },
  "bugs": {
    "url": "https://github.com/jarinfrench/rewrap-plus/issues"
  },
  "homepage": "https://github.com/jarinfrench/rewrap-plus#readme",
  "categories": ["Formatters"],
  "engines": {
    "vscode": "^1.122.0"
  },
  "main": "./dist/extension.js",
  "activationEvents": [],
  "capabilities": {
    "untrustedWorkspaces": {
      "supported": true
    }
  },
  "contributes": {
    "commands": [
      {
        "command": "rewrapPlus.wrapAtCursor",
        "title": "Wrap at Cursor",
        "category": "Rewrap+"
      },
      {
        "command": "rewrapPlus.wrapSelection",
        "title": "Wrap Selection",
        "category": "Rewrap+"
      },
      {
        "command": "rewrapPlus.wrapDocument",
        "title": "Wrap Document",
        "category": "Rewrap+"
      },
      {
        "command": "rewrapPlus.showResolvedConfig",
        "title": "Show Resolved Configuration",
        "category": "Rewrap+"
      }
    ],
    "keybindings": [
      {
        "command": "rewrapPlus.wrapAtCursor",
        "key": "alt+q",
        "when": "editorTextFocus && editorLangId in rewrapPlusSupportedLanguages"
      }
    ],
    "configuration": {
      "title": "Rewrap+",
      "properties": {
        "rewrapPlus.enable": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Global kill switch for Rewrap+. Turn this off to disable every wrap command and the range-formatting provider without hunting down each individual `#rewrapPlus.wrapComments#`/`#rewrapPlus.wrapStrings#`-style setting — the one to reach for when debugging a save pipeline with several formatters in it."
        },
        "rewrapPlus.columnLimit": {
          "type": ["number", "null"],
          "default": null,
          "scope": "resource",
          "markdownDescription": "Column to wrap at. `null` (the default) falls through to `#editor.rulers#` (language-scoped, then global), then `.editorconfig`'s `max_line_length`, then a built-in default of 80 — see the README's precedence table for the full chain. Supports a language-scoped override, e.g. `\"[python]\": { \"rewrapPlus.columnLimit\": 79 }`."
        },
        "rewrapPlus.rulerIndex": {
          "type": "number",
          "default": 0,
          "minimum": 0,
          "scope": "resource",
          "markdownDescription": "Which entry of `#editor.rulers#` to use as the column limit when `#rewrapPlus.columnLimit#` is `null` and more than one ruler is configured. Out of range falls back to the first ruler."
        },
        "rewrapPlus.wrapComments": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Wrap line and block comments."
        },
        "rewrapPlus.wrapStrings": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Wrap eligible string literals, subject to `#rewrapPlus.stringPolicy#`."
        },
        "rewrapPlus.stringPolicy": {
          "type": "string",
          "enum": ["off", "prose", "all"],
          "enumDescriptions": [
            "Never wrap strings, regardless of rewrapPlus.wrapStrings.",
            "Wrap only strings that score as prose-like under the heuristic (paths, URLs, regexes, dict keys, and similar are left alone). The conservative default.",
            "Wrap every eligible string, ignoring the prose heuristic."
          ],
          "default": "prose",
          "scope": "resource",
          "markdownDescription": "How aggressively to wrap string literals. See `#rewrapPlus.wrapStrings#`."
        },
        "rewrapPlus.docDialect": {
          "type": "string",
          "enum": ["auto", "google", "numpy", "sphinx", "jsdoc", "doxygen", "javadoc", "plain"],
          "enumDescriptions": [
            "Detect the dialect per docstring/doc comment.",
            "Google-style (Args:/Returns:/Raises: sections).",
            "NumPy-style (Parameters + ---------- underline).",
            "Sphinx/reST field lists (:param x:/:returns:).",
            "JSDoc-style (@param/@returns/@throws tags).",
            "Doxygen-style (\\param/@param, \\return/@return, \\brief, ...).",
            "Javadoc-style (@param/@return/@throws tags).",
            "Plain paragraph reflow only, no section structure."
          ],
          "default": "auto",
          "scope": "resource",
          "markdownDescription": "Documentation dialect for Python docstrings, JS/TS JSDoc comments, C++ Doxygen comments, and Java Javadoc comments. `auto` (the default) detects per docstring/comment rather than per file, since mixed conventions in one codebase are common; any other value forces that dialect regardless of its own shape."
        },
        "rewrapPlus.preserveIndentedBlocks": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Treat an already-indented block inside a comment or docstring (beyond a paragraph's first line) as verbatim rather than reflowing it — an aligned example or ASCII diagram is exactly what a wrong reflow guess mangles."
        },
        "rewrapPlus.respectEditorConfig": {
          "type": "boolean",
          "default": true,
          "scope": "resource",
          "markdownDescription": "Consult `.editorconfig`'s `max_line_length` as a precedence tier for `#rewrapPlus.columnLimit#` resolution. Parsed directly by this extension, independent of whether the separate EditorConfig extension is installed."
        },
        "rewrapPlus.balancedWrapping": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "markdownDescription": "Use minimum-raggedness (balanced) line breaking instead of the default greedy first-fit. Often visibly nicer for short comments/docstrings, at the cost of more computation."
        },
        "rewrapPlus.stringWrapInclude": {
          "type": "array",
          "items": { "type": "string" },
          "default": ["**"],
          "scope": "resource",
          "markdownDescription": "Glob patterns, matched against each file's path relative to its workspace folder (`**`, the default, matches every file), scoping *string-literal* wrapping to specific paths — lets string wrapping be trialled on one package or `docs/` before trusting it repo-wide. A file matching none of these patterns has string wrapping disabled regardless of `#rewrapPlus.wrapStrings#`. Does not affect comment or docstring wrapping (`#rewrapPlus.wrapComments#`). Use `#rewrapPlus.showResolvedConfig#` to check whether the current file is in scope."
        },
        "rewrapPlus.formatOnSave": {
          "type": "boolean",
          "default": false,
          "scope": "resource",
          "markdownDescription": "Wrap the whole document automatically before every save, independent of `#editor.formatOnSave#`/`#editor.defaultFormatter#`. Off by default: a whole-document reflow touches far more of the file than a typical code formatter, so it stays opt-in even after installing. Never delays a save — a parse failure or a wrap that doesn't finish quickly is skipped with a warning in the \"Rewrap+\" output channel, and the save proceeds either way. If another formatter (Black, Prettier, ...) also runs on save, ordering between them is controlled by VS Code's own `#editor.defaultFormatter#`/`#editor.codeActionsOnSave#`, not by this setting — see the README's format-on-save section."
        }
      }
    }
  },
  "scripts": {
    "build": "node ./scripts/build.mjs",
    "test": "vitest run",
    "pretest:integration": "npm run build && tsc -p tsconfig.test.json",
    "test:integration": "node ./out-test/runTest.js",
    "typecheck": "tsc -b tsconfig.json",
    "prepackage": "npm run build",
    "package": "vsce package --no-dependencies",
    "postpackage": "node ./scripts/verify-vsix-contents.mjs"
  },
  "dependencies": {
    "@rewrap-plus/engine": "*",
    "web-tree-sitter": "^0.26.13"
  },
  "devDependencies": {
    "@types/mocha": "^10.0.10",
    "@types/node": "^24.0.0",
    "@types/vscode": "^1.120.0",
    "@vscode/test-electron": "^3.1.0",
    "@vscode/vsce": "^3.9.2",
    "esbuild": "^0.28.2",
    "mocha": "^11.8.0",
    "ovsx": "^1.1.1",
    "vitest": "^4.1.11"
  }
}
