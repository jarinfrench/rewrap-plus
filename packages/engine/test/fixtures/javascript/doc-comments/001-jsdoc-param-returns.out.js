/**
 * Greets somebody by name, returning a friendly message
 * suitable for display in the UI.
 *
 * @param name The name of the person to greet, used
 *        verbatim in the message.
 * @returns A greeting message that includes the given name.
 */
function greet(name) {
  return "Hello, " + name;
}
