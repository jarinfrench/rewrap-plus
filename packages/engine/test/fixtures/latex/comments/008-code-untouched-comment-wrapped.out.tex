\section{Introduction}

This is body text that is not inside a comment and should remain completely untouched by commit 14's adapter, no matter how long this particular line running here is.

% This is a comment paragraph that
% should still wrap correctly at the
% configured column limit even though
% surrounding code is untouched.
