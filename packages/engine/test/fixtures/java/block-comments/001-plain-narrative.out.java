class Config {
    /*
     * This is a plain block comment with no Javadoc marker
     * at all, long enough that it needs to wrap across more
     * than one line at a sixty column limit.
     */
    void configure() {}
}
