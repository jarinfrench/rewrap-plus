class Report {
    String body = """
        This text block is deliberately long enough to exceed the configured column limit on its own, but text blocks are excluded from discovery entirely and must never be touched.
        """;
}
