def total_shipping_cost(order):
    """Compute the total shipping cost for an
    order.

    Returns:
    The total shipping cost in US dollars as a
    float, already rounded to two decimal places
    for display.

    Note:
    This note is written long enough on purpose so
    that it must be reflowed instead of left
    untouched.
    """
    return _compute(order)
