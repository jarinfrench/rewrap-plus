/**
 * Rewrap+ engine entry point.
 *
 * This package is intentionally empty of behavior at this stage (Phase 0 —
 * repository and scaffolding). Types, the parser layer, region discovery,
 * and the reflow pipeline are added in later phases.
 *
 * Hard rule: this package must never import `vscode`. See CONTRIBUTING.md.
 */
export {};
