{
  "name": "@rewrap-plus/engine",
  "version": "0.1.0",
  "private": true,
  "license": "MIT",
  "description": "Pure wrapping engine for Rewrap+: parsing, region discovery, reflow, and emit. Must never depend on vscode.",
  "type": "module",
  "main": "dist/src/index.js",
  "types": "dist/src/index.d.ts",
  "scripts": {
    "build": "tsc -b tsconfig.json",
    "test": "vitest run",
    "test:coverage": "vitest run --coverage",
    "typecheck": "tsc -b tsconfig.json"
  },
  "dependencies": {
    "web-tree-sitter": "^0.26.13"
  },
  "devDependencies": {
    "@vitest/coverage-v8": "^4.1.11",
    "fast-check": "^4.9.0",
    "vitest": "^4.1.11"
  }
}
